fun main() {
    val numbers = listOf(25, 12, 78, 45, 99)
    for (i in 0 until numbers.size - 1) {
        for (j in i + 1 until numbers.size) {
            if (numbers[i] > numbers[j]) {
                val temp = numbers[i]

                numbers
            }
        }
        println(numbers)
    }}
